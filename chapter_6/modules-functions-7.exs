# Float to string with 2 decimal digits (Erlang)
:io.format("~.2f", [5.123])

# Get value of an OS environment variable (Elixir)
System.get_env("USER")

# Return extension of a filename (Elixir)
Path.extname("dave/test.exs")

# Convert string w/ JSON into Elixir data structures. Just find don't install
# Answer: Poison library? https://github.com/devinus/poison

# Execute a command in your OS shell
System.cmd("ls", [])
